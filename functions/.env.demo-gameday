# Emulator-only overrides (loaded for project demo-gameday, never prod):
# the sweep's self-calls must hit the emulator's own functions, not the
# deployed production endpoints.
SELF_BASE=http://127.0.0.1:5101/demo-gameday/us-central1
